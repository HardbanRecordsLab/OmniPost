{
  "name": "omnipost-root",
  "private": true,
  "version": "6.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix app\" \"npm run dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "@types/node": "^20.11.30"
  }
}
